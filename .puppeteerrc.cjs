/**
 * Puppeteer configuration
 * Skip Chrome download since we'll use system-installed Chrome/Chromium
 */
module.exports = {
  // Skip downloading Chrome during npm install
  skipDownload: true,
};

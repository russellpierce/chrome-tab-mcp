# Chrome Tab Reader - Python Dependencies

# MCP Server dependencies
fastmcp>=0.1.0
requests>=2.31.0
python-dotenv

# HTTP Server dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Cross-platform config directory support (XDG Base Directory Specification)
platformdirs>=4.0.0

# Note: Install with uv (required):
#   uv pip install -r requirements.txt
#
# Or let uv handle dependencies automatically via PEP 723 inline metadata:
#   uv run chrome_tab_http_server.py
#   uv run chrome_tab_mcp_server.py

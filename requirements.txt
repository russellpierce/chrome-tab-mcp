# Chrome Tab Reader - Python Dependencies

# Optional: For proper XDG Base Directory Specification compliance on Linux
# and cross-platform config directory support
# If not installed, will fall back to manual implementation
platformdirs>=4.0.0

# FastAPI for HTTP API with automatic OpenAPI spec generation
fastapi>=0.104.0

# ASGI server for running FastAPI
uvicorn[standard]>=0.24.0

# Note: The HTTP server (chrome_tab_http_server.py) requires FastAPI and uvicorn.
# platformdirs is optional but recommended for better compliance with system standards.
#
# Install with: pip install -r requirements.txt
